const AllTransactions=()=>{
    // const queriedData = useQuery('allTransactions',getAllTranscations);
    // console.log(queriedData);
    return (
      <div>
        <h1>All Transactions</h1>
      </div>
    );
}
export default AllTransactions;